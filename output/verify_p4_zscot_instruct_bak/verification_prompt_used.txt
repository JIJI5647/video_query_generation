PROMPT VERSION: verification_prompt_p4_zscot_v2

TASK:
Watch the provided video clip(s). If several clips are given, treat them as one
continuous moment. For each query, judge three independent dimensions.

Use only the video clip(s) and the query text.

VIDEO ID: {video_id}
Round Index: {round_index}

{{include: verification_rules.txt}}

REASONING (do this for EACH query before deciding):
Step 1 — Watch: state what is actually visible and audible at the moment the
  query points to.
Step 2 — Relevance: is the query about one of the eight target emotions (or a
  clear synonym / its observable cue)?
Step 3 — Answerability: is that moment really present in the clip, and does the
  claimed emotion match what you saw/heard? Could an annotator locate it?
Step 4 — Quality: check English, single clear question, observable wording, no
  timestamps, no proper names.
Then set the three booleans from your reasoning, and put a brief version of the
reasoning in the "reasoning" field of each result.

QUERIES TO VERIFY:
{queries_json}

OUTPUT:
Return valid JSON only:
{
  "video_id": "{video_id}",
  "round_index": {round_index},
  "results": [
    {
      "query_id": "<query id>",
      "reasoning": "<brief step-by-step justification>",
      "relevance_pass": <true | false>,
      "answerability_pass": <true | false>,
      "query_quality_pass": <true | false>,
      "failure_reason": "<empty if all true; one short phrase otherwise>",
      "suggested_revision": "<empty unless wording-only fix>"
    }
  ]
}
