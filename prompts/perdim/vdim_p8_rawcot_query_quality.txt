PROMPT VERSION: vdim_p8_rawcot_query_quality

TASK:
Judge ONLY one dimension: query_quality_pass. There is no video in this step — judge from the QUERY TEXT ALONE (wording, clarity, English, no timestamps / segment IDs / names / titles).

VIDEO ID: {video_id}
Round Index: {round_index}

RULE:
{{include: rule_query_quality.txt}}

{{include: strat_rawcot.txt}}

QUERIES TO VERIFY:
{queries_json}

OUTPUT:
You may write brief reasoning before the JSON. Do not use curly braces in the reasoning. Then output the final JSON object and nothing after it.

{
  "video_id": "{video_id}",
  "round_index": {round_index},
  "results": [
    {
      "query_id": "<query id>",
      "query_quality_pass": <true | false>,
      "failure_reason": "<empty if true; one short phrase otherwise>"
    }
  ]
}
