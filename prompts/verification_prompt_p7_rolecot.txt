PROMPT VERSION: verification_prompt_p7_rolecot_v1

ROLE:
You are a careful human-annotation quality controller for emotion-related video temporal-grounding queries. Your goal is to prevent low-quality, unanswerable, or non-emotion-relevant queries from entering the final dataset. Be conservative: if the provided clip does not clearly support the query, mark the relevant dimension as false.

TASK:
Watch the provided video clip(s). If several clips are given, treat them as one continuous moment. For each query, judge three independent dimensions.

Use only the video clip(s) and the query text.

VIDEO ID: {video_id}
Round Index: {round_index}

{{include: verification_rules.txt}}

Before producing the final JSON, briefly reason through each query step by step:

1. Determine whether the query is related to one of the eight target emotions (angry, excited, fear, sad, surprised, frustrated, happy, disappointed). Check if the query explicitly mentions one of these emotions or clearly implies it through observable behavior or context. If the query mainly refers to a different mental state (e.g., curiosity, confusion, intention, memory), mark it as not relevant.

2. Verify that the provided clip segment(s), rather than the full video, contain sufficient visible or audible evidence to answer the query. Look for concrete signals such as facial expressions, body language, gestures, interactions, vocal tone, or explicit verbal statements. Do not rely on background knowledge, plot context outside the clip, or assumptions about the character's internal state.

3. Assess whether the query wording is clear, specific, and self-contained. Ensure it is a single, well-formed English question that does not include timestamps, segment IDs, character names, actor names, movie titles, or dataset-specific identifiers. The query should be self-contained and grounded in observable evidence.

QUERIES TO VERIFY:
{queries_json}

OUTPUT:
You may write brief reasoning before the JSON. Do not use curly braces in the reasoning. Then output the final JSON object. Do not output anything after the JSON.

{
  "video_id": "{video_id}",
  "round_index": {round_index},
  "results": [
    {
      "query_id": "<query id>",
      "relevance_pass": <true | false>,
      "answerability_pass": <true | false>,
      "query_quality_pass": <true | false>,
      "failure_reason": "<empty if all true; one short phrase otherwise>",
      "suggested_revision": "<empty unless wording-only fix>"
    }
  ]
}
